fastapi==0.128.8
starlette==0.49.1  # pinned: PYSEC fixes (remaining 2 advisories need starlette 1.x - blocked on FastAPI support)
PyJWT[crypto]==2.13.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
python-multipart==0.0.31
httpx==0.27.0
python-dotenv==1.2.2
openpyxl==3.1.2
pillow==12.3.0
pytesseract==0.3.13
reportlab==4.2.5
pypdf==6.14.2
python-docx==1.2.0
qrcode==7.4.2
# stdlib zoneinfo has no bundled IANA database on Windows (and on some minimal
# Linux images) - it falls back to this package automatically when present.
# Needed by routers/dashboards.py's My Agenda window conversion.
tzdata==2025.2
# sentry-sdk  # optional error tracking - install and set NEXUS_SENTRY_DSN to enable
