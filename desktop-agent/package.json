{
  "name": "plugin",
  "productName": "Plugin",
  "version": "0.8.5",
  "description": "Time & activity tracker for Greens Nexus â€” records the foreground app/window and a periodic screenshot while clocked in, per the disclosed monitoring policy. Visible 'Nexus Monitoring Active' tray indicator (no stop/exit), offline spooling with resume; on company PCs a Windows supervisor service (service/) auto-starts it, respawns it, and only IT admins can stop or remove it. Per-machine MSI under Program Files.",
  "main": "src/main.js",
  "author": "Greens Global",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder",
    "dist:win": "electron-builder --win",
    "dist:mac": "electron-builder --mac"
  },
  "dependencies": {
    "active-win": "^8.2.1"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3"
  }
}
